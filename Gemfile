source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in zap-rspec.gemspec
gemspec

group :test, :development do
  gem 'pry'
  gem 'rubocop'
end
